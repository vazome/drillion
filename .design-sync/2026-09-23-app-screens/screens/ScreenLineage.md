One task's lineage as a screen of its own.

Three columns (needs, this task, opens) joined by curves: `warn` dashed for a prereq not passed, `pass` solid for one passed, `control-edge` dashed for what this task opens. Each card opens its own lineage; Open NNN leads into the editor. Prereqs are information, never a gate.
