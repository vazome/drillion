Settings, the one dialog, over whatever screen was open.

A `scrim` with a 3px blur behind it. Five parts in the order a learner needs them: Editor (with the note that these live in this browser), Your data (three paths with Copy), Back up and Restore side by side, and the Danger zone on `danger-surface` in a `danger-edge` frame, its button disabled until `erase progress` is typed.
