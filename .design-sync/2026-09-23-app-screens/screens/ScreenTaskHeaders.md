The task header on two lines when a task has prereqs.

Line 1: number, title, active timer. Line 2: difficulty, path, status, Needs chips (▲ `warn` not passed, ✓ `pass` passed; titles dropped past two or past a 30-character one, kept in a tooltip), and Opens N tasks or Lineage on the right. Every chip opens that task's lineage.
