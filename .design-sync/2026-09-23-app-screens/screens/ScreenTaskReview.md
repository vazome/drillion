After a pass the editor pane becomes Review.

Compare, Yours or Reference; side by side or inline. Changed lines use `accent-tint` with `accent-line` word marks, never red and green: both versions passed. The spec pane narrows to 440px and says the solution is open; the passed banner sits under the diff.
