Review when a task edits several files (a future case; no task does yet).

A tab strip of the files you edited, each with its count of differing lines or a `pass` check when it matches; previous and next step through every difference across files; the summary line counts across all of them. Read-only files stay out. Past about five files, the tabs fold into a Files menu with the same counts.
