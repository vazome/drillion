[project]
name = "study"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "boto3>=1.43.69",
    "fastapi>=0.141.1",
    "httpx>=0.28.1",
    "ipykernel>=7.3.0",
    "langchain-core>=1.5.4",
    "moto>=5.2.2",
    "pytest>=9.1.1",
    "pytest-timeout>=2.4.0",
    "pytest-watcher>=0.6.3",
    "requests>=2.34.2",
    "responses>=0.26.2",
    "ruff>=0.16.2",
]

[tool.ruff.lint.per-file-ignores]
"study.py" = ["DTZ"]  # local wall-clock dates are intended for scheduling

[tool.pytest.ini_options]
python_files = ["ex_*.py", "test_*.py"]
testpaths = ["exercises", "rsample_drill"]
