image: ghcr.io/acme/api:2.3.1

# copied into the container as they are
resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    memory: 256Mi

# copied into the pod as it is; leave it empty to run on any node
nodeSelector: {}
